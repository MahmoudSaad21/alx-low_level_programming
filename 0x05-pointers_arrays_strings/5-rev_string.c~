#include "main.h"
/**
*print_rev - prints a string, in reverse
*@s: first string
*Return: nothing
*/
void print_rev(char *s)
{
int len = 0, count = 0;
while (s[len] != '\0')
len++;
for (count = len - 1; count >= 0; count--)
_putchar(s[count]);
_putchar(10);
}
